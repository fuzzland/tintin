#!/usr/bin/env node
const { run } = require("./tintin-helper-lib.js");

run("code", process.argv.slice(2)).catch((err) => {
  console.error(`[code] ${err && err.message ? err.message : String(err)}`);
  process.exit(1);
});
